data = {
  :list_id  => @list.id.to_s,
  :position => @list.group_associations.where(:group_id => @group.id).first.position.to_s
}

@list.channel.trigger_async "lists-insert_at", data, params[:socket_id]