= form_for Task.new, :url => group_list_tasks_path(group, list), :remote => true do |f|
  = f.text_field :title, :id => nil, :class => "new_task", :autocomplete => "off", :placeholder => "Create a task"
