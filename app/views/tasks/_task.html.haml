%li
  %div.task{ "data-id" => task.id }
    = link_to complete_task_path(task), :remote => true, :method => :put, :class => link_class(task) do
      = task.task
    %p
      = link_to "x", task_path(task), :remote => true, :method => :delete